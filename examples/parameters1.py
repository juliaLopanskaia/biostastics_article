N_per_cluster = 10; # number of experiments per cluster
N_clusters = 3; # number of clusters

true_exp_mean = 0.8; # a true value of experimental measurement (e.g. measurement with drug)
true_control_mean = 0.8; # a true value of control measurement (e.g. measurement without drug)

inter_day_SD = 0.25; # the inter day data varience
intra_day_SD = 0.3; # the intraday data varience
