N_clusters = 3; # number of clusters
N_per_cluster = 10; # number of experiments per cluster

true_exp_value = 1; # a true value of experimental measurement (e.g. measurement with drug)
true_control_value = 1; # a true value of control measurement (e.g. measurement without drug)

intra_day_SD = 0.3; # the intraday data varience
inter_day_SD = 0.15; # the inter day data varience

NN = 100
MAX_N_clusters = 11
MAX_N_per_cluster = 21

data_method = 'pool'
ttest_method = 'simple'
